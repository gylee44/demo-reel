# 검증 기록

## 2026-09-15 완성 영상의 자막 수정·계획 화면 정리

| 기능 단계 | 예상 결과 | 실제 결과 |
|---|---|---|
| 완성 영상의 자막·내레이션 수정 | 자막만 고치면 재촬영·재인증 없이 해당 장면만 재합성 | 통합 테스트로 `captureSceneIds`가 빈 배열이고 내레이션 외 계획이 원본과 동일함을 확인. 모든 장면이 성공한 영상에서만 허용하고, 변경이 없거나 원본 클립이 없으면 거부 |
| 보여줄 기능 추가 | 완성된 영상이 있으면 추가·변경된 장면만 다시 찍고 나머지 클립은 재사용 | 기존 복구 경로를 재사용하도록 연결. E2E 5개 통과 |
| 계획 검토 화면 정리 | 내레이션과 사람이 읽는 단계 요약이 먼저 보이고 동작·시간·대상 편집은 접힘 | E2E 5개 통과. 기존 라벨·버튼 접근 경로 유지. 스크린샷으로 두 화면 검수 |
| 공급자 오류 구분 | 크레딧 소진과 일시적 제한을 다르게 안내 | 단위 테스트 통과. 실제 OpenAI 키에서 429 사유가 `credit_balance_exhausted`임을 확인 |

자막 수정 경로는 저장된 계획에서 내레이션 문장만 바꿔 새 버전을 만들고, 서버가 직접 재촬영 범위를 빈 배열로 지정한다. 요청으로 재촬영을 유도할 수 없다. 재렌더 대상이 아닌 장면은 음성을 만들지 않으므로 자막 1개 수정에 전체 TTS 비용이 들지 않는다.

실제 키 확인: OpenAI 계정은 크레딧 충전 전까지 모든 호출이 429다. 충전 전 계정의 모델 목록에 gpt-5 계열이 없었으므로 충전 후 `OPENAI_PLANNER_MODEL`을 다시 확인해야 한다. 실제 유료 호출·음성 품질은 여전히 미검증이다.

## 2026-09-11 서비스 전환 검증

기준 입력은 별도 연구 노트 앱, 사용자 지정 로그인 필드, OpenAI 형식의 모의 응답, 로컬 S3 프로토콜 서버다. 기존 업무 앱·고정 계획·고정 음원 경로는 회귀 테스트에만 사용한다.

| 기능 단계 | 예상 결과 | 실제 결과 |
|---|---|---|
| 실행 엔진 분리 | 사용자 지정 로그인과 실제 DOM 기반 계획 검증. 키 없으면 호출 0회 | 단위·통합 97개 + 기존 E2E 3개 통과 |
| 계정·저장 백엔드 | 타인 작업 거부, 로그아웃·비밀번호 변경 시 세션 무효화, 원자적 한도, 서명 업로드·복원 | 단위·통합 105개 + 기존 E2E 3개 통과 |
| 서비스 모드·프론트·운영 설정 | 고정 경로 없이 녹화·음성·저장 완료, 회원가입·빈 연결 폼·모바일 오류 표시 | 단위 76개 + 통합 34개 + E2E 5개 = **115개 통과**, 타입 검사·빌드 통과 |

최종 전체 검사 기록: `output/checks/2026-09-10T18-49-10.523Z.json` (KST 9월 11일 최종 실행).
서비스 모드 실행 기록: `output/service-test/verification.json`. 실제 다른 앱의 브라우저 3장면, 음성 HTTP 요청 3회, **51,000ms MP4**, S3 객체 저장 성공, 종료 후 인증 자료 삭제. 음성 응답은 테스트용 16초 오디오이며 실제 OpenAI 음성 품질을 검증한 결과는 아니다. 로컬 앱 검사에서만 브라우저 생성 함수를 주입하고, 운영 코드는 공개 IP 프록시를 사용한다.

경계·실패 검사: IPv4/IPv6 내부·예약 주소, 혼합 DNS 응답, 루프백 CONNECT 차단, 키 누락, AI 거부·불완전 응답, 관측되지 않은 locator, 잘못된 인증, 교차 계정 접근, 만료·손상된 객체, 중복 한도 경쟁, 회원 세션 취소를 확인했다.

실패를 확인하고 수정한 부분:

- 다른 앱 테스트의 한글 인코딩 선언 누락으로 입력 이름이 깨졌다. 실제 접근성 트리로 원인을 확인하고 테스트 앱 응답에 UTF-8을 지정했다.
- AWS SDK의 `aws-chunked` 전송 본문을 모의 저장소가 일반 본문으로 읽었다. 전송 형식을 확인한 후 모의 서버에서 분할 프레임을 복원했고, 원본 바이트·서명·손상 검사를 그대로 유지했다.
- Zod의 `oneOf`를 OpenAI Structured Outputs의 `anyOf` 형식으로 변환한다. 모델 응답은 별도로 Plan 스키마·관측 증거·실행 정책을 통과해야 한다.

공개 네트워크 실검사: macOS Chromium sandbox를 켠 서비스 브라우저에서 공개 IP 검증 프록시를 통해 `https://example.com`을 읽고 `Example Domain` 제목을 확인했다. 근거는 `output/service-test/public-egress.json`이다.

실제 OpenAI 유료 호출, 외부 S3 계정, Linux 컨테이너 sandbox, Vercel 배포 환경은 미검증이다. 설정값이 존재한다는 heartbeat를 실제 공급자 연결 성공으로 취급하지 않는다.


유닛 테스트와 단위 테스트는 같은 의미로 사용한다. 순수 규칙은 단위 테스트, 실제 DB·API 경계는 통합 테스트, 사용자 흐름과 녹화는 브라우저 E2E로 검증한다.

기능이 완성될 때마다 `pnpm check`로 현재 구현된 전체 테스트·타입 검사·빌드를 실행한다. PoC는 실제 컨테이너·브라우저·영상 검사와 장애 주입을 별도로 수행한다. 아래 예상 결과를 실행 전에 기록하고 이후 실제 결과를 덧붙인다.

| 단계 | 실행 전 예상 | 실제 결과 |
|---|---|---|
| 1. 공유 계약·복구·시간 규칙 | 정상 계획은 통과. 빈 장면·중복 ID·잘못된 대상·의존성·길이 초과는 거부. 독립 장면 복구와 의존 장면 확장이 정확함 | 타입 검사 + 단위 테스트 34/34 통과. 라이브러리 빌드 설정을 동기화한 뒤 검사 실행 |
| 2. 테스트 앱·API·DB·큐 | 실제 로그인·업무 생성. 승인 전 실행·타인 조회·중복 요청·오래된 편집 거부 또는 기존 응답 반환 | 타입 검사 + 전체 55/55 통과(단위 42·통합 13). 빈 JSON 삭제 요청을 최소 재현으로 확인·수정. 실제 PostgreSQL·pg-boss 사용 |
| 3. 촬영·렌더 | 로그인 후 실제 녹화. 커서·한국어 음성·자막, 규격에 맞는 MP4. 데이터 변경 중단은 자동 반복 금지 | 전체 63/63 통과. 실제 링크 이름에 포함된 화살표를 DOM 관측으로 확인·수정. 실제 브라우저 녹화·MP4 합성 검사 포함. 전체 3장면 PoC는 5단계에서 추가 검증 |
| 4. 화면·E2E | 계획 폼 편집 → 승인 → 진행 → 결과 재생. 오류·복구 범위 표시 | 타입 검사·빌드 + 단위·통합 68/68 + E2E 3/3 통과. 실제 3장면 영상 재생, 잘못된 로그인, 잘못된 계획 검증. 탐색 후 커서가 초기 위치로 돌아가는 문제를 수정하고 회귀 테스트 추가 |
| 5. 전체 PoC | 같은 초기 데이터·계획으로 3회 성공. 부분 재촬영 때 다른 클립 해시 보존. 최종 영상 전체 검수 | 수정본 반복 3/3 통과. 세 영상 50.333초. 부분 재촬영·만료 후 재검증·실제 저장 직후 프로세스 종료 검증 모두 통과. 세 영상 프레임 검수·규격 검사 수행. 팀원 전체 청취·시각 검수는 남음 |
| 6. 최종 경계·회귀 | 새로고침 후 같은 승인 작업 재조회. 검증 만료 후에도 성공 장면을 보존하고 실패 장면만 재검증. 저장 완료 직후 중단 시 데이터 1개·자동 반복 0회 | 23:24 KST 최종 타입 검사·빌드·단위/통합 69/69·브라우저 E2E 3/3 통과(총 72개). 취소 시 인증 즉시 폐기도 통과. 전체 검사 원본: `output/checks/2026-09-09T14-24-52.877Z.json` |

성능·품질은 측정 전 추정으로 성공을 주장하지 않는다. 외부 앱 3개 검증과 실제 LLM·유료 TTS는 현재 고정 시나리오 PoC 이후 단계다.

고정 음원 기준 입력은 `NARRATIONS`의 같은 세 문장 묶음이다. Yuna 속도 190에서 13.670/13.073/14.078초, 속도만 145로 바꾼 뒤 16.302/15.493/16.689초로 측정했다. 최종 영상 길이는 실제 조작 시간과 마무리 여유까지 포함해 별도로 검사한다.

## 실패 원인과 검증 범위

- 최초 단위·통합 테스트의 코드 변환 경로에서는 커서가 있었지만 실제 `tsx` 실행에서는 브라우저에 없는 `__name` 보조 함수가 직렬화됐다. 브라우저에 전달하는 함수를 독립적인 코드로 수정하고, 녹화 전에 커서의 실제 가시성을 필수 검사한다. 동일 조건의 실제 녹화 프레임에서 커서 표시를 확인했다.
- 첫 중단 실험은 클릭 직후 프로세스를 종료해 HTTP 요청이 DB에 도착하기 전 종료될 수 있었다. 목표 반례인 **저장 완료·실행 기록 미완료** 구간을 재현하도록 실제 생성 결과가 나타난 직후, 실행 결과를 DB에 기록하기 전에 종료한다. 실패 기준을 완화한 것이 아니라 주입 시점을 정확하게 고정했다.
- 화면 수정 중 개발 서버의 자동 새로고침으로 진행 화면이 사라진 테스트 1건이 있었다. 최종 E2E는 고정된 프로덕션 빌드를 제공하는 preview 서버를 사용하며, 실제 사용자 새로고침도 별도로 검증한다.
- Docker 런타임·리눅스 브라우저 sandbox는 아직 검증하지 않았다. Compose YAML은 파싱·형식을 확인했으며 이것을 실행 성공으로 취급하지 않는다.
- 팀원 2인의 세 영상 전체 청취·시각 검수, 실제 TTS 후보 비교, 외부 앱 9회 테스트는 미실행이다. 자동 규격 검사와 프레임 검수가 이 항목을 대체하지 않는다.

## 최종 로컬 PoC 측정

2026-09-09 23:14:28~23:21:37 KST, macOS arm64·Node 24.19.0·Playwright 1.63.0·ffmpeg 9.0.1·고정 Yuna 음원. 실행 기록 원본은 `output/poc/report.json`, 대표 영상은 `output/poc/demo-reel.mp4`에 있다.

| 실행 | 결과 | 영상 길이 | 생성 예약부터 완료까지 |
|---|---|---|---|
| 1 | 3장면 성공 | 50.333초 | 100.233초 |
| 2 | 3장면 성공 | 50.333초 | 86.457초 |
| 3 | 3장면 성공 | 50.333초 | 78.280초 |

세 실행의 의미상 Plan SHA-256은 `64cafbc2ffe7df96827fc16a1b324880cf5279bfd0715db2ffda508eefda88ec`로 같았다. 생성 ID·타임스탬프·인증 참조는 비교에서 제외했다. 화면의 미세한 실행 시각이 달라 영상 파일 해시까지 동일하다는 뜻은 아니다.

- 부분 복구: 승인 후 조회 링크 이름을 바꿔 `overview`만 실패시켰다. `create`·`complete`의 원본 해시와 렌더 클립 ID가 복구 전후 동일했다. 기존 검증 결과를 만료시킨 경우에도 조회 장면만 재검증·재촬영해 최종 영상이 성공했다.
- 중단: 실제 업무 생성 결과가 보인 직후 워커가 exit 86으로 종료됐다. 영구 기록은 `unknown`, 저장된 업무는 **1개**, 큐 재시도 한도·실행 횟수는 **0**이었다. 새 워커는 작업을 `needs_action`으로 전환했고 재인증·앱 상태 확인 전 복구 요청은 409로 거부했다.
- 인증: 세 정상 실행·부분 복구·중단 후 인증 레코드 폐기를 확인했다. 원본·완성 영상에는 로그인·비밀번호 입력 화면을 포함하지 않는다.
- 규격: 세 영상 모두 1280×720, H.264, 30/1fps, AAC 48kHz 모노. 실제 동영상 재생·시간 진행을 E2E에서 확인했다.
- 프레임 검수: 세 영상의 2초 간격 프레임과 대표 영상 주요 시점·마지막 프레임을 확인했다. 커서, 실제 입력·저장·완료 결과, 한국어 자막이 보인다. 자동 스캔에서 0.15초 이상 검은 구간 0건, -45dB 이하가 1초 이상 지속되는 무음 구간 0건이다. 이 검사는 짧은 흰 화면·음성 발음·매 프레임의 품질을 보장하지 않는다.

## 최초 테스트 계획 대비 범위

| 시나리오 | 현재 증거·남은 범위 |
|---|---|
| T01 | 실제 API·큐·브라우저 3회 PoC 및 UI E2E |
| T02 | 정적 계약·API 입력 경계 테스트 |
| T03 | 시간 상한 규칙·고정 음원 길이 측정. 실제 유료 TTS 길이 초과는 후속 |
| T04 | 실제 브라우저 0개·복수 대상 검사, 실제 DOM 변경 장애 주입 |
| T05 | 실제 프로세스 종료·재시작·데이터 1개·자동 반복 0회 |
| T06 | 독립 조회 장면 1개만 재촬영, 나머지 2개 클립 보존 |
| T07 | 의존성·자원 영향 범위·잘못된 순서의 단위 테스트. 외부 앱 의존 흐름은 후속 |
| T08 | 실제 로그인·쿠키 상태 재사용·인증 만료 차단. 외부 인증 방식은 후속 |
| T09 | 실제 DB 트랜잭션의 중복 예약·승인 버전 고정·낡은 편집 거부 |
| T10 | 부분 복구에서 기존 클립으로 최종 합성. 변경 내레이션의 실제 TTS 재생성은 후속 |
| T11 | 타인 작업 접근·CSRF·허용 범위 밖 브라우저 요청 차단. 외부 DNS·연결 IP 격리는 Docker·외부 앱 단계 |
| T12 | 한도·예약·취소·작업 새로고침. 디스크 부족 주입·자동 보관 정리는 후속 |
